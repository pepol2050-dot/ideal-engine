# دليل الإعداد النهائي — Google Sheets (أسهل طريقة، 3 دقايق، مجانية 100%)

من غير Firebase، من غير كونسول، من غير أكواد. بس Google Sheets عادي.

---

## الخطوة 1: إنشاء الجدول
1. روحي على **sheets.google.com** وسجّلي دخول بحساب جوجل بتاعك.
2. دوسي **+ فارغة (Blank)** لإنشاء شيت جديد.
3. سمّيه من فوق: **محتوى المتفوقين**.
4. من قائمة **File → Import**، اختاري تبويب **Upload**، وارفعي ملف `sheet-template.csv` اللي بعتهولك — هيملأ العناوين وأمثلة جاهزة تلقائيًا (اختاري "Replace current sheet" وقت الاستيراد).

## الخطوة 2: منع الأخطاء الإملائية (Data Validation)
### أ) عمود "النوع"
1. اختاري كل العمود B (النوع) بالدوس على حرف العمود فوق.
2. من القائمة: **Data → Data validation**.
3. اختاري **Dropdown**، واكتبي القيم الأربعة: `فيديو` ، `صورة` ، `شرح` ، `PDF`.
4. احفظي (Done).

### ب) عمود "المجموعة"
1. اعملي تبويب/شيت جديد في نفس الملف اسمه **Groups** (من زرار + تحت الشاشة).
2. افتحي ملف `group-names-list.txt` اللي بعتهولك، انسخي كل الأسماء (500 اسم)، والصقيهم في عمود A بالتبويب الجديد (من الخلية A1).
3. ارجعي للتبويب الأساسي، اختاري عمود A كله (المجموعة).
4. **Data → Data validation → Dropdown (from a range)** واختاري `Groups!A1:A500`.
5. احفظي.

دلوقتي أي حد هيملا الجدول هيختار من قائمة منسدلة بس، مفيش احتمال غلطة إملائية تمنع ظهور الدرس.

## الخطوة 3: صلاحيات المعلمين الثلاثة
1. دوسي **Share** (أعلى يمين الشاشة).
2. تحت "Add people"، اكتبي إيميلات جوجل بتاعة المعلمين التلاتة، وخليهم **Editor**.
3. تحت "General access"، سيبيها **Restricted** (يعني بس اللي اتضافوا يقدروا يعدّلوا) — دي حماية حقيقية بحساب جوجل، مش باسورد بس.
4. دوسي **Done**.

## الخطوة 4: نشر الجدول عشان الموقع يقرأه
1. من قائمة **File → Share → Publish to web**.
2. من القائمة المنسدلة الأولى، اختاري التبويب الأساسي (مش Groups).
3. من القائمة التانية، اختاري **Comma-separated values (.csv)**.
4. دوسي **Publish**، وأكدي بـ OK.
5. هيديكي رابط طويل — انسخيه بالكامل.

## الخطوة 5: ربط الرابط بالموقع
1. افتحي ملف `assets/site-config.js` من ملفات الموقع.
2. حطي الرابط اللي نسختيه مكان `PASTE_PUBLISHED_CSV_LINK_HERE` في `SHEET_CSV_URL`.
3. ارجعي لتاب الجدول في المتصفح، وانسخي رابطه العادي من فوق (شكله زي: `https://docs.google.com/spreadsheets/d/xxxxx/edit`).
4. حطيه مكان `PASTE_YOUR_GOOGLE_SHEET_LINK_HERE` في `SHEET_EDIT_URL`.
5. احفظي الملف.

## الخطوة 6: الرفع النهائي
ارفعي فولدر الموقع بالكامل على **Netlify** بنفس طريقة السحب والإفلات اللي عملناها قبل كده.

---

## ✅ للتجربة فورًا (حتى قبل رفع Netlify)
افتحي `login.html` من على جهازك مباشرة (دبل كليك)، سجّلي دخول كمعلم بباسورد `511994`، دوسي "افتحي الجدول"، ضيفي سطر تجريبي، بعدين افتحي `login.html` تاني كطالب لنفس المجموعة بباسورد `10102020` — هتلاقي الدرس ظاهر خلال ثواني.

## ملاحظة أمان
- دخول الموقع نفسه (باسورد الطلاب والمعلمين) بسيط ومناسب لموقع فصول دراسية، زي ما طلبتِ بالظبط.
- تعديل المحتوى محمي بحساب جوجل حقيقي (خطوة 3) — أقوى من أي باسورد مشترك.
